import 'package:flutter/material.dart';

const double mainWidth = double.infinity,
    mainHeight = 150.0,
    initSpawnObstacle = 60.0,
    initGameSpeed = 13.0,
    gravity = 1.0;

const Color backgroundMaincolor = Colors.grey;

double gameSpeed = initGameSpeed, spawnObstacle = initSpawnObstacle;
